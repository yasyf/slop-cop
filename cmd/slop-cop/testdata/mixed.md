# Release notes

There are two supported modes. In an era of shipping faster than ever, the report is generated by the analyzer rather than by the caller, which means every caller has to make a decision about which of the two available modes it wants before the run starts.

The command line tool allows you to utilize the second mode.
