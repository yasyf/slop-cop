package rules

import "github.com/yasyf/slop-cop/internal/types"

// Base is the clarity layer: rules about whether a sentence can be read once
// and understood, rather than about whether it sounds like an LLM.
var Base = []types.ViolationRule{
	{
		ID:           "long-sentence",
		Name:         "Long Sentence",
		Category:     types.CategoryBase,
		Description:  `A sentence past 40 words, counting each parenthetical or quoted span as one word. Past that length the reader has to go back to find the subject.`,
		Tip:          `Split it at a joint the sentence already has: a semicolon, a colon, a coordinating conjunction. Keep each piece above roughly 9 words — three sentences shorter than that in a row trades this violation for staccato-burst.`,
		CanRemove:    false,
		Color:        "#9ca3af",
		BgColor:      "rgba(156,163,175,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Split sentences longer than 40 words at an existing clause boundary, keeping each piece above roughly 9 words.`,
		LLMDirective: `Do not write a sentence longer than 40 words — split it at a clause boundary, keeping each piece above roughly 9 words.`,
	},
	{
		ID:           "long-paragraph",
		Name:         "Long Paragraph",
		Category:     types.CategoryBase,
		Description:  `A paragraph running past 6 sentences. A paragraph should carry one idea; past six sentences it is usually carrying three.`,
		Tip:          `Find where the second idea starts and break there. If every sentence is load-bearing and none of them belong together, the paragraph is a list — set it as one.`,
		CanRemove:    false,
		Color:        "#6b7280",
		BgColor:      "rgba(107,114,128,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Break paragraphs longer than 6 sentences where the second idea starts, or set them as a list.`,
		LLMDirective: `Do not write a paragraph longer than 6 sentences — break it where the second idea starts.`,
	},

	{
		ID:           "passive-voice",
		Name:         "Passive Voice",
		Category:     types.CategoryBase,
		Description:  `A passive that names its actor after "by": "the report is generated by the analyzer." The actor is right there, demoted behind the verb.`,
		Tip:          `Move the actor in front of the verb. "The report is generated by the analyzer" → "the analyzer generates the report." Adverbial phrases like "by default" and "by design" name no actor and are not flagged.`,
		CanRemove:    false,
		Color:        "#4b5563",
		BgColor:      "rgba(75,85,99,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Rewrite by-agent passives as active voice: move the actor named after "by" in front of the verb.`,
		LLMDirective: `Do not write a passive that names its actor after 'by' — put the actor in front of the verb.`,
	},
	{
		ID:           "padded-verb",
		Name:         "Padded Verb",
		Category:     types.CategoryBase,
		Description:  `An auxiliary frame padding out a verb: "allows you to," "is able to," "can be used to," "has the ability to."`,
		Tip:          `Cut to the verb. "The flag allows you to skip the check" → "the flag lets you skip the check," or better, "the flag skips the check." "Lets you" is the fix, so it is never flagged.`,
		CanRemove:    false,
		Color:        "#a1a1aa",
		BgColor:      "rgba(161,161,170,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Replace padded verb frames ("allows you to", "is able to", "can be used to", "has the ability to") with the plain verb or "lets you".`,
		LLMDirective: `Do not pad a verb with 'allows you to', 'is able to', 'can be used to', or 'has the ability to' — use the plain verb or 'lets you'.`,
	},
	{
		ID:           "nominalization",
		Name:         "Nominalization",
		Category:     types.CategoryBase,
		Description:  `A verb buried in a noun behind a light verb: "make a decision," "perform an analysis," "make use of."`,
		Tip:          `Use the verb the noun is hiding. "Make a decision" → "decide." "Perform an analysis of the logs" → "analyze the logs."`,
		CanRemove:    false,
		Color:        "#71717a",
		BgColor:      "rgba(113,113,122,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Replace light-verb frames with the verb they bury: "make a decision" → "decide", "make use of" → "use".`,
		LLMDirective: `Do not bury a verb in a noun phrase ('make a decision', 'perform an analysis') — use the verb directly.`,
	},

	{
		ID:           "missing-hyphen",
		Name:         "Missing Hyphen",
		Category:     types.CategoryBase,
		Description:  `A compound modifier left open in front of the noun it modifies: "a command line tool," "an open source project."`,
		Tip:          `Hyphenate the compound when it modifies a noun — "a command-line tool" — and leave it open when it stands as a predicate: "the tool is open source."`,
		CanRemove:    false,
		Color:        "#52525b",
		BgColor:      "rgba(82,82,91,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Hyphenate a compound modifier sitting in front of its noun: "command line tool" → "command-line tool".`,
		LLMDirective: `Hyphenate a compound modifier in front of its noun ('command-line tool'); leave it open in predicate position ('the tool is open source').`,
	},
	{
		ID:           "expletive-opener",
		Name:         "Expletive Opener",
		Category:     types.CategoryBase,
		Description:  `A sentence opening on "there is" or "there are," which parks an empty placeholder in the subject slot and delays the real one.`,
		Tip:          `Promote the real subject. "There are two ways to run it" → "You can run it two ways." Existential negations ("There are no guarantees.") assert something and are not flagged. An enumerating opener that continues on the same line ("There are three modes: fast, slow, off.") is a known false positive — rewrite it as "slop-cop has three modes:" or ignore the hit.`,
		CanRemove:    false,
		Color:        "#a8a29e",
		BgColor:      "rgba(168,162,158,0.15)",
		RequiresLLM:  false,
		RewriteHint:  `Rewrite "there is"/"there are" openers so the real subject leads the sentence.`,
		LLMDirective: `Do not open a sentence with 'there is' or 'there are' — promote the real subject to the front.`,
	},

	{
		ID:               "agentless-passive",
		Name:             "Agentless Passive",
		Category:         types.CategoryBase,
		Description:      `A passive that drops an actor the reader could have been given: "the file was moved," in a piece that knows who moved it.`,
		Tip:              `Name the actor and put it in front of the verb. When the actor is genuinely unknown or genuinely beside the point, the passive is the right call and this rule leaves it alone.`,
		CanRemove:        false,
		Color:            "#78716c",
		BgColor:          "rgba(120,113,108,0.15)",
		RequiresLLM:      true,
		LLMTier:          types.LLMTierSentence,
		LLMDetectionHint: `A passive verb phrase with no "by" agent whose actor is recoverable from the surrounding text but left unstated — "the file was moved" in a passage that has already said who moves files. Do not flag a predicate adjective that only looks passive ("is deprecated", "is required", "is disabled"). Do not flag a passive that keeps the paragraph's existing topic in the subject slot. Do not flag one whose actor is genuinely unknown or genuinely irrelevant. Do not flag quoted error, log, or command output. Flag at most 2 per paragraph, choosing the ones whose actor is most clearly recoverable.`,
		RewriteHint:      `Name the actor in an agentless passive and move it in front of the verb, unless the actor is genuinely unknown or irrelevant.`,
		LLMDirective:     `Do not use a passive that hides an actor the reader could be told — name the actor and put it in front of the verb.`,
	},

	{
		ID:               "term-consistency",
		Name:             "Term Drift",
		Category:         types.CategoryBase,
		Description:      `One thing called two names in a single document — "the base layer" in one section, "the clarity pass" in the next. A reader who meets the second name assumes a second thing.`,
		Tip:              `Pick one name and use it everywhere. If both names must appear, introduce the alias once, explicitly, and then keep to the primary.`,
		CanRemove:        false,
		Color:            "#57534e",
		BgColor:          "rgba(87,83,78,0.15)",
		RequiresLLM:      true,
		LLMTier:          types.LLMTierDocument,
		LLMDetectionHint: `Two or more names used for one referent across the document, where a reader could take them for two different things. The referent must be a concrete technical noun — a flag, a file, a component, a step, a command. Do not flag a repeated image, metaphor, or conceptual frame: dead-metaphor owns imagery. Do not flag an alias the document introduces explicitly ("the base layer (the clarity pass)"). Flag the later, less common variant rather than the first use.`,
		RewriteHint:      `Use one name per referent throughout the document — replace the drifting variant with the document's primary term.`,
		LLMDirective:     `Do not give one referent two names — choose the document's primary term and use it everywhere.`,
	},
}

// All is the canonical ordered list of every rule. Slop first is load-bearing:
// the LLM prompt numbers rules by position, so appending Base leaves the
// existing sentence-tier prompt numbering unchanged.
var All = append(append([]types.ViolationRule{}, Slop...), Base...)
