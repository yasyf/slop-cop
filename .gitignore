# Build output
/slop-cop
/dist/
/bin/

# Test + coverage
*.test
*.out
coverage.*

# Go
vendor/

# Env + local
.env
.env.*
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.swp
*.swo

# Cursor
.cursor/

# Agent memory dirs (Serena etc.)
.serena/

# Bootstrap output of the plugin test; regenerated on demand
/bin/

# Zig cross-compile cache: setup-zig points ZIG_*_CACHE_DIR here, so a restored
# cache lands in the worktree before goreleaser's dirty-tree check — ignore it.
.zig-cache/
